# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
<<<<<<< HEAD
  pull_request:
    branches: [ "main" ]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  deploy:
    # The type of runner that the job will run on
=======

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  deploy:
>>>>>>> 02cbdc394acf478ef40e3d755e698a50f409db70
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Deploy to EC2
        run: |
<<<<<<< HEAD
          echo Add other actions to build,
          echo test, and deploy your project.

      - name: Setup SSH
        run : |
          mkdir -p~/.ssh/
          echo "${{ secrets.AWS_EC2_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

      - name: Deploy to aws

        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.AWS_EC2_IP }}
          username: ${{ secrets.AWS_EC2_USER }}
          key: ${{ secrets.AWS_EC2_KEY }}

          script: |
            cd /home/ubuntu/<project name>
           
            # we need to pull
            git pull origin main

            # we need to activate our env
            source env/bin/activate

            # we need to install our requirements
            #pip install -r requirements.txt

            # we need to run our migrations
            python3 manage.py migrate

            # restart gunicorn to refresh the server
            sudo systemctl restart gunicorn
=======
          # this is used to connect with ec2 via ssh 
          ssh -o StrictHostKeyChecking=no ${{ secrets.AWS_EC2_USER }}@${{ secrets.AWS_EC2_IP }} << 'EOF'

          cd /home/ubuntu 

          source env/bin/activate

          git pull origin main

          # cd /home/ubuntu/<folder_name>
          # pip install -r requirement.txt 

          sudo supervisorctl restart all

          EOF
          
>>>>>>> 02cbdc394acf478ef40e3d755e698a50f409db70
